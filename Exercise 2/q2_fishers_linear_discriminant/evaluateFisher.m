function y = evaluateFisher(X, w, w0)
	% Evaluate the Fisher linear discriminant classifier
	%
	% INPUT:
	% X        : Input data        (num_samples x dim)
	% w        : Classifier vector (dim x 1)
	% w0       : Threshold         (scalar)
	%
	% OUTPUT:
	% y        : labels (1 x num_samples)

	% insert your code here

end
